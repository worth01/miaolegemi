[build]
  buildCommand = "cd server && npm install && npx prisma generate"

[deploy]
  startCommand = "cd server && npx prisma generate && npx prisma db push && npx tsx src/index.ts"
  healthcheckPath = "/"
  healthcheckTimeout = 30
